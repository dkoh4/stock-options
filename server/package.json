{
  "name": "stock-options-server",
  "version": "1.0.0",
  "description": "Backend server for stock options data visualization",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "init-db": "ts-node src/db/init.ts",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  }
} 